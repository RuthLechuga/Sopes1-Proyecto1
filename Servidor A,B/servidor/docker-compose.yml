version: '2.2'
services: 
  flask-mongo:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - "/proc:/proc"
    network_mode: host
    depends_on:
      - mongo
  
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    expose:
      - "27017"
    network_mode: host
